# Optional shared question-feedback backend.
# Copy to .env and fill these values from Supabase Project Settings > API.
# The anon key is intended for browser use; database access is restricted by RLS.
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_ANON_KEY=
